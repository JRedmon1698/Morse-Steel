import React from 'react';

const AddTeamMembersToProject = ({ projectDetails, setProjectDetails, team }) => {

  return (
    <div>{console.log(team)}
    radio buttons
    </div>
  );
};

export default AddTeamMembersToProject;